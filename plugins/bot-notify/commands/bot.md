---
description: Bind this session to the shared notification room (no arg = bind), or stop
argument-hint: "[stop|status]"
allowed-tools: ["Bash"]
---

!`bash "${CLAUDE_PLUGIN_ROOT}/scripts/bot-cmd.sh" "$ARGUMENTS"`

Relay the single line above to the user verbatim. Do not add commentary.
